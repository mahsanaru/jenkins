

pipeline {
    agent {
        // Define agent details here
    }
    stages {
        stage('Example stage 1') {
            steps {
                sh("kubectl get pods")
            }
        }
    }
}
